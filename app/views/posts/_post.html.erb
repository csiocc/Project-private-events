<div class="bg-white rounded-xl shadow hover:shadow-md transition border border-gray-100 p-5">

  <div class="flex justify-between items-start">
    <div class="flex-1">
      <h2 class="text-xl font-semibold text-blue-700 hover:underline">
        <%= link_to post.title, post_path(post) %>
      </h2>
      <p class="text-gray-600 mt-1 line-clamp-1">
        <%= truncate(strip_tags(post.body), length: 120) %>
      </p>
    </div>

    <div class="ml-4 text-right text-sm text-gray-500 whitespace-nowrap">
      <p><%= time_ago_in_words(post.updated_at) %> ago</p>
    </div>
  </div>

  <div class="flex justify-between items-center mt-3 text-sm text-gray-500">
    <div class="flex items-center space-x-2">
      <div class="flex items-center justify-center w-8 h-8 rounded-full bg-gray-200 text-gray-700 font-medium">
        <%= post.user.name[0].upcase %>
      </div>
      <span><%= post.user.name %></span>
    </div>

    <div class="flex items-center space-x-4">
      <span class="flex items-center space-x-1">
        💬 <%= post.comments.count %>
      </span>
      
      <span class="flex items-center space-x-1">
        👁️ <%= rand(50..500) %> <!-- Placeholder -->
      </span>

    </div>
  </div>
</div>
