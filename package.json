{
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.18",
    "@rails/activestorage": "^8.0.300",
    "cloudinary": "^2.7.0",
    "esbuild": "^0.25.10",
    "flowbite": "^3.1.2",
    "sortablejs": "^1.15.6",
    "tailwindcss": "^4.1.14"
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets",
    "build:watch": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --watch --public-path=/assets"
  }
}
